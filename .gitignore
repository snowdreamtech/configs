# This is a sample .gitignore file for a project using SSH keys and configuration files.
# It is recommended to keep sensitive files out of version control.
# This file should be placed in the root of your repository.

# SSH keys - Ignore all private and public key files
id_*

# Known hosts file may contain sensitive server fingerprints
known_hosts

# Ignore SSH agent socket files
.ssh/agent*

# Session files often contain temporary sensitive data
.ssh/sessions/*

# User-specific SSH client configuration
# Uncomment if you want to ignore your SSH config
# config

# GnuPG sensitive files
# Private keys, keyrings, and trust database
.gnupg/*.gpg
.gnupg/random_seed
.gnupg/*.kbx
.gnupg/trustdb.gpg
.gnupg/tofu.db
.gnupg/pubring.kbx*
.gnupg/private-keys-v1.d/
# GPG agent and pinentry sockets/logs
.gnupg/S.*
.gnupg/*.log

# Vim sensitive files
# Swap files
*.swp
*.swo
*.swn
# Backup files
*~
# Vim info file
.viminfo
# NetRW history file
.netrwhist
# Vim undo directory
.vim/undo/
# Session files
Session.vim
Sessionx.vim
# View files (for preserving folds etc.)
*.vim.view